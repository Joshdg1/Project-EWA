import { describe, it, expect } from 'vitest'

// import { mount } from '@vue/test-utils'
import View from  '../views/admin/administrator/View.vue'

describe('test', () => {
    it('does', () => {
        // mount(View, {
        //
        // });
        expect(true);
    });
});